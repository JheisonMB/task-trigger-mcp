fn main() {
    eprintln!("⚠️  task-trigger-mcp is deprecated. Install agent-canopy instead:");
    eprintln!("   cargo install agent-canopy");
    eprintln!("   https://crates.io/crates/agent-canopy");
    std::process::exit(1);
}
